// use std::path::Path;
// use directories::ProjectDirs;

// const PACKAGE_IDENTIFIER: String = String::from("com.starshot.app");

// pub fn return_config_directory() -> &Path {
    
//     let proj = ProjectDirs::from(PACKAGE_IDENTIFIER[0], PACKAGE_IDENTIFIER[1], PACKAGE_IDENTIFIER[2]).unwrap();
    
//     return proj.config_dir() 
// }